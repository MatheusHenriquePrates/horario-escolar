# PostgreSQL Database
DATABASE_URL="postgresql://usuario:senha@localhost:5432/nome_database"

# JWT Secret (gere uma chave forte com: openssl rand -base64 32)
JWT_SECRET="sua-chave-secreta-aqui"

# Porta do servidor
PORT=3001

# CORS Origins (URLs permitidas, separadas por vírgula)
CORS_ORIGINS="http://localhost:5174"

# Ambiente
NODE_ENV="development"
